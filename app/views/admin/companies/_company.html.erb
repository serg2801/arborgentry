<tr>
  <td><%= company.title %></td>
  <td>
  	<%= link_to "Edit", edit_admin_company_path(company), remote: true %> / 
	<%= link_to "Delete", admin_company_path(company), method: :delete, data: { confirm: "You sure?" }, remote: true %>
  </td>
</tr>
